import { h } from 'preact';
import PageWrapper from '../../components/UI/PageWrapper';

const Home = () => (
	<PageWrapper>
		<h1>Home</h1>
		<p>This is the Home component.</p>
	</PageWrapper>
);

export default Home;
